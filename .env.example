# Copy to `.env` (gitignored) and fill in. Real environment variables
# take precedence over values in `.env`.

# Admin UI password; unset or empty disables /admin entirely.
#ADMIN_PASSWORD=your-password

# Log filter (EnvFilter syntax). Defaults to "info,sqlx=warn".
#RUST_LOG=info,proxy_converter=debug
